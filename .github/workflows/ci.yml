name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  rust:
    name: Rust (fmt, clippy, build, test)
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: 1.83.0
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Format (rustfmt)
        run: cargo fmt --all -- --check

      - name: Lint (clippy)
        run: cargo clippy --workspace --exclude chagashi-fuzz --all-targets --all-features -- -D warnings

      - name: Build
        run: cargo build --workspace --exclude chagashi-fuzz --all-features

      - name: Test
        run: cargo test --workspace --exclude chagashi-fuzz --all-features

  property-tests:
    name: Property Tests (chagashi)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: 1.83.0

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run property-based tests
        env:
          PROPTEST_CASES: 256
          PROPTEST_FORK: false
        run: cargo test -p chagashi -- --test-threads=1

